# Peronsal
Personal work that is based only for learning nothing else. 
$ ssh-keygen =t rsa -b 4096 -C "daniel@beyondgames.co"
ls -al ~/.ssh


This is a test for making a commit to GitHub

For this particular part of the test, I'm trying to see about committing directly from sourcetree master vs. a branch. 

This is a sub-text test
